# server.port: 4300
# management.context-path: /mgmt

spring:
  application:
    name: auth
  cloud:
    config:
      uri: http://manage:7100
      fail-fast: true
      password: ${MANAGE_SERVICE_PASSWORD}
      username: ${MANAGE_SERVICE_USER}
#logging:
#  level:
#    org.springframework: DEBUG