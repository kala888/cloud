dependencies {
  compile libs.bootAdmin
  // compile libs.springcloud.config
//  compile libs.springcloud.consul
  // compile libs.springcloud.amqp
  // compile libs.jolokia
  testCompile libs.test
}
